# GenHook Application Configuration
# Note: For production deployment, set port = 443 for direct HTTPS
# or keep port = 8000 and use reverse proxy (recommended)

[server]
host = 0.0.0.0
port = 8000
reload = true

[sl1]
api_url = https://ec2-3-106-238-172.ap-southeast-2.compute.amazonaws.com/api/alert
username = em7admin
password = Ver33tiger
timeout = 30
retry_attempts = 3

[logging]
level = INFO
format = %(asctime)s - %(name)s - %(levelname)s - %(message)s
file = /var/log/genhook.log

[threading]
max_workers = 50
queue_size = 10000
processing_timeout = 30