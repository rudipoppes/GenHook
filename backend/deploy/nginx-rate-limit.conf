# Rate limiting configuration for GenHook
# This needs to be added to the http block in /etc/nginx/nginx.conf

# Add this inside the http { } block in /etc/nginx/nginx.conf:
limit_req_zone $binary_remote_addr zone=webhook_limit:10m rate=100r/m;